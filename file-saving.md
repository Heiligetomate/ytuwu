# File Saving Process

To show this process, I will show an example of the saving of an entire channel.

- A channel has a vec with singles and two vecs with playlists in them
- Every playlist has a vec with media objects in them (same as the singles)
- Every media can have one or n streams in them
- Every media can have one or zero thumbnails in them

## Streams and Thumbnails

The main saving function for streams is [save_media_stream()](src/downloader/streams/util.rs#L9-L24). It takes a path and a file name. The path to the file is generated by taking the given path and adding the file name. The ending is automatically added.

This function gets used by every struct implementing the [MediaStream](src/downloader/streams/core.rs#L7-L12) trait. An example of the [save()](src/downloader/streams/core.rs#L9) function can be found [here](src/downloader/streams/audio.rs#L25-L28). As you can see, it is just a wrapper for [save_media_stream()](src/downloader/streams/util.rs#L7-L12).

A [Thumbnail](src/downloader/thumbnail.rs#L13-L17) is basically structured like a media stream. It has the pure bytes in it and a file name. The [save()](src/downloader/thumbnail.rs#L28-L41) function takes a path and adds the file name to it. The extension is always `.png`.

## Downloaded Media

There are two different types of downloaded media: media with one stream and media with n streams. Both types can have a thumbnail and both types have the same metadata.

Both types have three saving methods:

- [save_thumbnail()](src/downloader/media/downloaded.rs#L49-L52) — a wrapper for the thumbnail save discussed above
- [DwnMedia::save_media_stream()](src/downloader/media/downloaded.rs#L44-L46) — a wrapper for saving a single stream
- [DwnBundleMedia::save_media_streams()](src/downloader/media/downloaded.rs#L80-L94) — saves all streams and appends an index to the file name if there are duplicate extensions (e.g. `x.webm`, `x.mp3` but `y.webm`, `y-1.webm`)
- [save_full()](src/downloader/media/downloaded.rs#L54-L58) — calls both of the functions discussed above

## Downloaded Playlists

[DwnList](src/downloader/playlist/downloaded.rs#L15-L18) has a vec of [DwnMedia](src/downloader/media/downloaded.rs#L16-L20).
[DwnBundleList](src/downloader/playlist/downloaded.rs#L21-L24) has a vec of [DwnBundleMedia](src/downloader/media/downloaded.rs#L23-L27).

Both have two saving methods:

- [DwnList::save()](src/downloader/playlist/downloaded.rs#L55-L60) — calls [DwnMedia::save_media_stream()](src/downloader/media/downloaded.rs#L44-L47) on each media object
- [DwnBundleList::save()](src/downloader/playlist/downloaded.rs#L32-L37) — calls [DwnBundleMedia::save_media_streams()](src/downloader/media/downloaded.rs#L80-L94) on each media object
- [save_with_dir()](src/downloader/playlist/downloaded.rs#L39-L46) — creates an additional directory in the given path and calls [save()](src/downloader/playlist/downloaded.rs#L32-L37) with the new path

## Downloaded Channel

[DwnChannel](src/downloader/channel/downloaded.rs#L13-L18) has a vec of [DwnList](src/downloader/playlist/downloaded.rs#L15-L18).
[DwnBundleChannel](src/downloader/channel/downloaded.rs#L20-25) has a vec of [DwnBundleList](src/downloader/playlist/downloaded.rs#L21-L24).

First, three folders are created:

- `path/singles/`
- `path/eps/`
- `path/albums/`

After that, a save function is called which does the following:

- Calls [DwnMedia::save_media_stream()](src/downloader/media/downloaded.rs#L44-L47) / [DwnBundleMedia::save_media_streams()](src/downloader/media/downloaded.rs#L80-L94) for each single
- Calls [save_with_dir()](src/downloader/playlist/downloaded.rs#L39-L46) for each EP or album
